1) At the moment every version update resets the users local storage numbers (Future Improvement: If real users (or staff/caregivers) start using this daily and saving custom costs, you won't want to wipe their saved inputs just to add new default categories. You might later implement a light "schema migration" script that merges old user values into new key structures.)
1) Need to add an "Empty" button to depopulate the calculator,
2) Maybe Need to add a report button to send out the data??,
3) Need to reorganise all the groups/items,
4) Need to add functionality of greyed out cost and consumption suggestions,
5) Need to add the functionality of doubled up items (e.g. water),
6) Need to warn the user when something looks crazy (like if the cost is out of range or the consumption number hasn't been altered?),
7) Maybe make an icon,
